html {
    head {
    }
    body {
        h1 "Hello, world!"
        br // line breaks
        p class="big" attribute1 attribute2 "Rendered"
        div {
            p "I'm a child!"
        }
        /- p "Ignored paragraph"
        // The last argument is the content,
        // so, to create en empty disabled button,
        // you must specify an empty string
        button disabled ""
        button disabled "you can't press me"
    }
}
