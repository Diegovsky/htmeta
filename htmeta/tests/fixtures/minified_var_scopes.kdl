html {
    body {
        $text "Hello, World"
        h1 "$text"
        div {
            $text "$text!"
            p "$text"
            $text "Olá, mundo!"
            p "$text"
        }
        h2 "$text"
    }
}
